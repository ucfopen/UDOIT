sentry:
  dsn: '%env(SENTRY_DSN)%'

  options:
    environment: '%env(APP_ENV)%'
    # Enable performance monitoring (adjust sampling rate as needed)
    traces_sample_rate: 0.1
    send_default_pii: true
