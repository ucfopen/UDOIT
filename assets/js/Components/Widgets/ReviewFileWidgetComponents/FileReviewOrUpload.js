import React from 'react'

const FileReviewOrUpload = ({
    activeFile,
    toggleMarkReview,
    handleDrop,
    handleKeyPress,
    handleDragOver,

}) => {
  return (
    <div>FileReviewOrUpload</div>
  )
}

export default FileReviewOrUpload